# Generated by `just plugins` and committed as-is. The bytes are pinned by hash
# in the signed manifest, and the loader is patched by that recipe — reformatting
# either would mean the committed artifact no longer matches what produced it.
src/lib/wasm/letras-pkg/
src/lib/wasm/letras2-pkg/
src/lib/wasm/letras3-pkg/

build/
.svelte-kit/

# Signed exactly as stored: reformatting either would invalidate the signature.
src/lib/plugin/manifest.json
src/lib/plugin/manifest.sig

# jsQR, vendored from upstream and wrapped in an ES-module shim. Reformatting it
# would make the committed file something other than what was published, for no
# benefit — nobody edits it, and diffing it against a fresh `npm pack` is how it
# gets re-vendored.
static/qr/

# Generated by `just plugins` and committed as-is. The bytes are pinned by hash
# in the signed manifest, and the loader is patched by that recipe — reformatting
# either would mean the committed artifact no longer matches what produced it.
src/lib/wasm/letras-pkg/

build/
.svelte-kit/
